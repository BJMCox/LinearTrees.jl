var documenterSearchIndex = {"docs":
[{"category":"section","location":"trees/#Tree-fitting","page":"Tree fitting","text":"fit_tree grows one linear model tree. The loss determines the prediction scale, the selection rule determines node choice, and split search determines which thresholds are considered.","title":"Tree fitting"},{"category":"section","location":"trees/#How-a-tree-predicts","page":"Tree fitting","text":"A row follows one path through the tree. Each node adds a constant or a linear function of one feature to the score. The loss link maps that score to a response, such as a regression value or a probability.\n\nKind Contribution Branches\nCON A constant None\nLIN One line in one feature One continuing path\nPCON A constant on each side of a threshold Two\nBLIN Two lines that meet at the threshold Two\nPLIN Two independent lines Two\n\nThe default BIC rule selects among these forms using a penalized weighted least-squares objective. For nonquadratic losses, that objective is a local approximation. Loss functions explains the fitting steps.\n\nLIN nodes do not increase branching depth. Nodes along a path can combine effects from several features, even in a shallow tree. BLIN enforces continuity at its own split, but a whole tree need not be continuous.","title":"How a tree predicts"},{"category":"section","location":"trees/#Control-model-size","page":"Tree fitting","text":"Start with the defaults and tune against validation data:\n\nKeyword Default Effect\nmax_depth 12 Maximum branching depth\nmin_fit 10 Minimum total row weight to consider another node model\nmin_leaf 5 Minimum total row weight on each side of a split\nmin_sum_hessian 1.0 Stop when total loss curvature is too small\nmax_lin_chain 10 Limit consecutive unsplit linear nodes\n\nIncreasing min_leaf or reducing max_depth usually restricts complexity. The fitted size also depends on the selection rule and the data. There is no pruning step after growth.\n\nusing LinearTrees, Random\n\nrng = Xoshiro(17)\nX = rand(rng, 200, 3)\ny = X[:, 1] .+ 2 .* (X[:, 2] .> 0.5) .+ 0.1 .* randn(rng, 200)\ntree = fit_tree(X, y; max_depth = 4, min_leaf = 10)\nround.(predict(tree, X[1:4, :]); digits = 3)\n4-element Vector{Float64}:\n 0.463\n 2.86\n 2.589\n 2.691\n\nUse features to restrict fitting to selected columns. Prediction still takes the original matrix layout:\n\nrestricted = fit_tree(X, y; features = [1, 2], max_depth = 4)\nround.(predict(restricted, X[1:4, :]); digits = 3)\n4-element Vector{Float64}:\n 0.463\n 2.86\n 2.589\n 2.691","title":"Control model size"},{"category":"section","location":"trees/#Frequency-weights","page":"Tree fitting","text":"Pass weights to assign each row a frequency. Weight 2 represents two copies of a row. Weights affect the fit, BIC's effective sample size, and the min_fit and min_leaf gates.\n\nw = ones(size(X, 1))\nw[1:20] .= 2\nweighted = fit_tree(X, y; weights = w, max_depth = 4)\nround.(predict(weighted, X[1:4, :]); digits = 3)\n4-element Vector{Float64}:\n 0.462\n 2.859\n 2.586\n 2.689\n\nWeights must be finite and nonnegative, with a positive total. Zero-weight rows are excluded. Rescaling all weights can change model selection and stopping, so weights are not normalized automatically.","title":"Frequency weights"},{"category":"section","location":"trees/#Categorical-features","page":"Tree fitting","text":"At the matrix interface, encode levels as positive integer codes and identify their columns with categorical. Reuse the same encoding for prediction.\n\ngroup = repeat([1.0, 2.0, 3.0, 4.0], 50)\nXcat = hcat(X[:, 1], group)\nycat = Xcat[:, 1] .+ 2 .* (group .== 3)\ncategorical_tree = fit_tree(Xcat, ycat; categorical = [2], max_depth = 3)\nround.(predict(categorical_tree, [0.2 1.0; 0.2 3.0]); digits = 3)\n2-element Vector{Float64}:\n 0.2\n 2.2\n\nCategorical nodes split sets of levels and fit constants on the two sides. Within each node, levels are ordered by mean working response, then the ordered partitions are searched. An unseen level routes right.\n\nUse the table interface for automatic categorical encoding. Exact and local-bin search support categorical features. Hybrid search supports numeric features only.","title":"Categorical features"},{"category":"section","location":"trees/#Truncation","page":"Tree fitting","text":"The default truncate = true applies two limits:\n\nEach node evaluates its linear term within the feature range seen in that node.\nThe accumulated score stays within loss-specific bounds learned from the training target.\n\nThese limits restrain extrapolation. truncate = false disables both limits. Linear terms can then extrapolate beyond the observed range.\n\nscore returns scores on the loss scale. score(tree, X; clip = false) removes score clipping but retains feature truncation if enabled during fitting. SHAP values explain this unclipped score.","title":"Truncation"},{"category":"section","location":"trees/#Selection-and-search","page":"Tree fitting","text":"rule = BIC() is the standalone default. GainRule provides ridge penalties and a minimum split gain, and is the rule used by boosting. MinDeviance is a low-level rule for comparisons and experiments.\n\nThe default ExactSearch checks every eligible numeric threshold in a node. It is exact for that local search, not a global optimization over trees. Performance describes two approximate search methods.","title":"Selection and search"},{"category":"section","location":"guide/#Getting-started","page":"Getting started","text":"This tutorial fits a regression tree, measures its error on unseen rows, and then fits a binary classifier. It uses only LinearTrees and Julia standard libraries.","title":"Getting started"},{"category":"section","location":"guide/#Prepare-the-data","page":"Getting started","text":"The matrix interface takes a numeric matrix X and a target vector y. Each row corresponds to one target. Keep the feature order the same when predicting.\n\nThe example combines a linear trend, a threshold effect, and noise:\n\nusing LinearTrees, Random, Statistics\n\nrng = Xoshiro(42)\nX = rand(rng, 400, 3)\ny = 2 .* X[:, 1] .- X[:, 2] .+ 3 .* (X[:, 3] .> 0.6) .+\n    0.1 .* randn(rng, 400)\n\ntrain, test = 1:300, 301:400\nXtrain, ytrain = X[train, :], y[train]\nXtest, ytest = X[test, :], y[test]\n(size(Xtrain), length(ytrain), size(Xtest))\n((300, 3), 300, (100, 3))\n\nThe rows were sampled independently, so this leaves an independent test sample. For time series or grouped observations, split according to that structure. Fit preprocessing on training rows alone.\n\nFeature values must be finite and numeric at this interface. Encode categorical features as described in Tree fitting, or use the table interface. Handle missing values before fitting.","title":"Prepare the data"},{"category":"section","location":"guide/#Fit-a-regression-tree","page":"Getting started","text":"tree = fit_tree(Xtrain, ytrain; max_depth = 4)\nyhat = predict(tree, Xtest)\nround.(yhat[1:5]; digits = 3)\n5-element Vector{Float64}:\n -0.162\n  0.007\n  3.939\n -0.056\n  3.961\n\nfit_tree uses MSE and BIC by default. BIC balances fit against node complexity. max_depth caps branching depth. A tree may stop earlier when another split does not improve its selection score.","title":"Fit a regression tree"},{"category":"section","location":"guide/#Measure-error-on-unseen-rows","page":"Getting started","text":"Compare the test error with a constant baseline fitted on the same training rows:\n\nrmse = sqrt(mean(abs2, yhat .- ytest))\nbaseline_rmse = sqrt(mean(abs2, mean(ytrain) .- ytest))\n(tree_rmse = round(rmse; digits = 3),\n    baseline_rmse = round(baseline_rmse; digits = 3))\n(tree_rmse = 0.099, baseline_rmse = 1.655)\n\nUse validation data or cross-validation to choose settings such as max_depth and min_leaf. Reserve the test data for the final assessment.","title":"Measure error on unseen rows"},{"category":"section","location":"guide/#Fit-binary-probabilities","page":"Getting started","text":"The direct binary interface uses targets 0 and 1. Logistic returns the probability of target 1:\n\nprobability = inv.(1 .+ exp.(-4 .* (X[:, 1] .- X[:, 2])))\nybinary = Float64.(rand(rng, 400) .< probability)\n\nclassifier = fit_tree(Xtrain, ybinary[train], Logistic(); max_depth = 3)\np = predict(classifier, Xtest)\nround.(p[1:5]; digits = 3)\n5-element Vector{Float64}:\n 0.22\n 0.422\n 0.49\n 0.23\n 0.816\n\nChoose a threshold when a decision is needed. A threshold of 0.5 is a starting point. Different costs for false positives and false negatives can justify another value.\n\nlabels = p .>= 0.5\nmean(labels .== ybinary[test])\n0.8\n\nFor named labels and probability columns with a stored class order, use the classifier wrapper. For more classes, see Loss functions.","title":"Fit binary probabilities"},{"category":"section","location":"guide/#Continue-from-here","page":"Getting started","text":"Tree fitting explains node models, weights, categorical features, and extrapolation. Boosting builds an ensemble with early stopping. Interpretation shows how to inspect a fitted model.","title":"Continue from here"},{"category":"section","location":"api/#API-reference","page":"API reference","text":"The matrix API returns a tree or ensemble. StatsAPI wrappers add table encoding and stored training data. MLJ models integrate with machines and resampling. Start with Getting started for a complete fitting workflow.","title":"API reference"},{"category":"section","location":"api/#Fit-and-predict","page":"API reference","text":"Use fit_tree for one tree and fit_boost for an ensemble. predict returns responses, while score returns values on the loss scale.","title":"Fit and predict"},{"category":"section","location":"api/#Fitted-models-and-node-kinds","page":"API reference","text":"These types describe the stored model. Fit models through the functions above.","title":"Fitted models and node kinds"},{"category":"section","location":"api/#Model-selection-and-split-search","page":"API reference","text":"Selection rules compare node models. Search policies choose numeric thresholds. See Tree fitting and Performance for their distinct roles.","title":"Model selection and split search"},{"category":"section","location":"api/#Loss-types-and-functions","page":"API reference","text":"See Loss functions for target domains and prediction scales.","title":"Loss types and functions"},{"category":"section","location":"api/#The-LossFunctions.jl-adapter","page":"API reference","text":"","title":"The LossFunctions.jl adapter"},{"category":"section","location":"api/#Interpretation","page":"API reference","text":"These functions describe fitted scores. See Interpretation for reconstruction identities and the effect of clipping.","title":"Interpretation"},{"category":"section","location":"api/#StatsAPI-and-Tables.jl","page":"API reference","text":"","title":"StatsAPI and Tables.jl"},{"category":"section","location":"api/#MLJ","page":"API reference","text":"","title":"MLJ"},{"category":"section","location":"api/#Printing-and-serialisation","page":"API reference","text":"","title":"Printing and serialisation"},{"category":"section","location":"api/#Index","page":"API reference","text":"","title":"Index"},{"category":"function","location":"api/#LinearTrees.fit_tree","page":"API reference","text":"fit_tree(X, y, loss=MSE(); kwargs...) -> LinearTree\n\nFit a linear model tree to the rows of numeric matrix X and target vector y. Columns are features. Targets must satisfy the domain of loss; feature values must be finite. Use predict for response-scale predictions and score for scores on the loss scale.\n\nKeywords\n\nweights=nothing: nonnegative frequency weights, with a positive total. The default gives every row weight one. Zero-weight rows are excluded.\ncategorical=Int[]: columns containing positive integer category codes.\nrule=BIC(): node selection rule. See BIC and GainRule.\nsplit_search=ExactSearch(): numeric threshold search. BinnedSearch and HybridSearch offer approximate search for scalar-score losses.\nmax_depth=12: maximum branching depth. Unsplit LIN nodes add no depth.\nmin_fit=10: minimum total row weight for another model-selection step.\nmin_leaf=5: minimum total row weight on each side of a split.\nmin_sum_hessian=1.0: stop below this total Hessian, summed over score coordinates.\nmax_lin_chain=10: maximum number of consecutive unsplit linear nodes.\ntruncate=true: bound score accumulation and each node's feature extrapolation.\ntruncation_factor=3: parameter passed to scorebound. Must be at least one.\nfeatures=1:size(X, 2): feature columns available for fitting. Prediction still expects the original matrix layout.\nnthreads=Threads.nthreads(): maximum number of available Julia threads to use.\nniter=5: node-refit iterations for non-smooth losses such as MAD and Quantile. Smooth losses ignore this keyword.\npresort=nothing: optional Int32 matrix of stable sorted row indices, one column per original feature. Used by exact and local-bin search.\n\nNodes add score increments along a path. Model selection uses a quadratic surrogate for nonquadratic losses. Logistic updates also check true training loss before accepting a full step. No pruning pass follows growth.\n\nFor table inputs and stored encoders, use LinearTreeRegressorFit or LinearTreeClassifierFit through fit.\n\n\n\n\n\n","title":"LinearTrees.fit_tree"},{"category":"function","location":"api/#LinearTrees.fit_boost","page":"API reference","text":"fit_boost(X, y, loss = MSE(); nrounds = 100, eta = 0.1, max_depth = 5,\n          min_fit = 10, min_leaf = 5, min_sum_hessian = 1.0,\n          lambda_slope = 1.0, lambda_intercept = 1.0, gamma = 0.0,\n          subsample = 1.0, colsample = 1.0, rng = Random.default_rng(),\n          weights = nothing, categorical = Int[], truncate = true,\n          Xval = nothing, yval = nothing, wval = nothing, patience = 10,\n          split_search = ExactSearch(), nthreads = Threads.nthreads()) -> LinearBoost\n\nSecond-order gradient boosting (Guryanov 2019) with the linear model tree as base learner. Each round computes the gradient and Hessian of loss at the ensemble score, freezes them into a Frozen target, and fits one tree with GainRule(lambda_slope, lambda_intercept, gamma), depth max_depth, and the row and column samples drawn from rng. The tree's raw score times eta is added to the ensemble. With Xval/yval, the validation deviance is recorded per round and fitting stops after patience rounds without improvement, keeping the trees up to the best round. Exact and node-local binned searches presort X once. HybridSearch prepares global bin IDs once from all positive-weight training rows, then selects the sampled rows' IDs each round. It supports numeric features and scalar losses. Sampled-out rows have weight zero for that tree only. history holds the per-round deviance and the ensemble's validated flag says whether it is the validation deviance, and so whether early stopping was in play.\n\nFor Quantile and MAD the frozen Hessian is the IRLS weight at the ensemble score, so a round is one IRLS step, not an exact L1 fit.\n\n\n\n\n\n","title":"LinearTrees.fit_boost"},{"category":"function","location":"api/#StatsAPI.predict","page":"API reference","text":"predict(tree_or_ensemble, X; nthreads=Threads.nthreads())\n\nPrediction on the response scale, linkinv(tree_or_ensemble.loss, score).\n\n\n\n\n\npredict(tree_or_ensemble, X; nthreads=Threads.nthreads())\n\nSoftmax override: n × K matrix of class probabilities, linkinv applied row by row to the K-1-vector score.\n\n\n\n\n\n","title":"StatsAPI.predict"},{"category":"function","location":"api/#LinearTrees.predict!","page":"API reference","text":"In-place Softmax prediction into an n × K matrix.\n\n\n\n\n\n","title":"LinearTrees.predict!"},{"category":"function","location":"api/#LinearTrees.score","page":"API reference","text":"score(tree_or_ensemble, X; clip=true, nthreads=Threads.nthreads())\n\nRaw path sum per row on the link scale. clip=false returns the additive unclipped sum. Rows split into nthreads contiguous blocks when there are at least PARALLEL_MIN_ROWS of them; each row writes only its own output slot, so the result matches the serial loop exactly.\n\n\n\n\n\nscore(tree_or_ensemble, X; clip=true, nthreads=Threads.nthreads())\n\nSoftmax override: n × (K-1) matrix of raw reference-class logits.\n\n\n\n\n\n","title":"LinearTrees.score"},{"category":"type","location":"api/#LinearTrees.LinearTree","page":"API reference","text":"Fitted tree. lo, hi are the stored first-truncation bounds on the score scale. base is the SHAP empty-coalition value. truncate == false disables both clamps at prediction.\n\n\n\n\n\n","title":"LinearTrees.LinearTree"},{"category":"type","location":"api/#LinearTrees.LinearBoost","page":"API reference","text":"LinearBoost{T,V,L}\n\nA gradient boosting ensemble of Frozen-loss linear model trees. The score is f0 + eta Σ_t score(trees[t], x), clamped to [lo, hi] (the base loss's scorebound on the training target) when truncate, then passed through the base loss's link. history[t] is the validation deviance after round t when validation data was given, else the training deviance; validated says which, so a reader can tell whether early stopping was in play. Build with fit_boost.\n\n\n\n\n\n","title":"LinearTrees.LinearBoost"},{"category":"function","location":"api/#LinearTrees.nrounds","page":"API reference","text":"Number of trees in the ensemble.\n\n\n\n\n\n","title":"LinearTrees.nrounds"},{"category":"type","location":"api/#LinearTrees.Node","page":"API reference","text":"One node of a linear model tree. Coefficients have type V, which is the feature type T for scalar-score losses and SVector{K-1,T} for softmax. Categorical split nodes keep a packed left-level mask in the tree's mask pool at words catstart:catstart+catwords-1. Numeric nodes have catwords == 0.\n\n\n\n\n\n","title":"LinearTrees.Node"},{"category":"type","location":"api/#LinearTrees.ModelKind","page":"API reference","text":"Per-node model kind. LIN nodes have one child and add no depth.\n\n\n\n\n\n","title":"LinearTrees.ModelKind"},{"category":"constant","location":"api/#LinearTrees.CON","page":"API reference","text":"Constant node: one intercept, no split.\n\n\n\n\n\n","title":"LinearTrees.CON"},{"category":"constant","location":"api/#LinearTrees.LIN","page":"API reference","text":"Single line across the whole node: a x + b. One child, no added depth.\n\n\n\n\n\n","title":"LinearTrees.LIN"},{"category":"constant","location":"api/#LinearTrees.PCON","page":"API reference","text":"Two constants, one per side of a threshold.\n\n\n\n\n\n","title":"LinearTrees.PCON"},{"category":"constant","location":"api/#LinearTrees.BLIN","page":"API reference","text":"Broken line, continuous at the threshold: basis [x, 1, max(x - t, 0)].\n\n\n\n\n\n","title":"LinearTrees.BLIN"},{"category":"constant","location":"api/#LinearTrees.PLIN","page":"API reference","text":"Two independent lines, one per side of a threshold.\n\n\n\n\n\n","title":"LinearTrees.PLIN"},{"category":"type","location":"api/#LinearTrees.BIC","page":"API reference","text":"PILOT's BIC rule: n log(dev/n) + dof(kind) log(n) with dof per kind (con, lin, pcon, blin, plin). The defaults are the paper's (1, 2, 5, 5, 7).\n\n\n\n\n\n","title":"LinearTrees.BIC"},{"category":"type","location":"api/#LinearTrees.MinDeviance","page":"API reference","text":"Test-only rule: pick the lowest surrogate deviance among kinds, never stop on con.\n\n\n\n\n\n","title":"LinearTrees.MinDeviance"},{"category":"type","location":"api/#LinearTrees.GainRule","page":"API reference","text":"GainRule(; lambda_slope = 1.0, lambda_intercept = 1.0, gamma = 0.0)\n\nBoosting's selection rule (Guryanov 2019, eq. 12 and 13). Offers con, pcon, and plin. The regularised deviance of a fit is `Σ h (z − a x − b)²\n\nlambdaslope a² + lambdaintercept b²`, obtained by adding the two ridges\n\nto the Gram sums in fit_lin and fit_con. A split's score is its children's regularised deviance plus gamma per coefficient coordinate, so a split is taken exactly when its gain over the parent's constant fit exceeds gamma. BIC's dof penalty and dmin floor play no part.\n\n\n\n\n\n","title":"LinearTrees.GainRule"},{"category":"type","location":"api/#LinearTrees.SplitSearch","page":"API reference","text":"Policy for choosing the numeric thresholds evaluated during tree fitting.\n\n\n\n\n\n","title":"LinearTrees.SplitSearch"},{"category":"type","location":"api/#LinearTrees.ExactSearch","page":"API reference","text":"ExactSearch()\n\nEvaluate every eligible numeric threshold. This is the default split search.\n\n\n\n\n\n","title":"LinearTrees.ExactSearch"},{"category":"type","location":"api/#LinearTrees.BinnedSearch","page":"API reference","text":"BinnedSearch(; nbins=64, refine=true)\n\nApproximate numeric split search for scalar-score losses. Build at most nbins equal-count bins within each node, keeping tied values together. Evaluate bin edges using sufficient statistics of the original feature values and responses. Weights retain their frequency meaning. Categorical search remains exact.\n\nWith refine=true, scan all eligible row boundaries in the two bins adjacent to the winning coarse split. No refinement runs when a constant or unsplit line wins. Nodes with at most nbins rows use exact search.\n\nThis can change the chosen model and predictions. It does not guarantee the exact optimum or an error bound. nbins must be at least two. Vector-score losses, including Softmax, require ExactSearch().\n\n\n\n\n\n","title":"LinearTrees.BinnedSearch"},{"category":"type","location":"api/#LinearTrees.HybridSearch","page":"API reference","text":"HybridSearch(; nbins=64)\n\nApproximate numeric split search for scalar-score losses. Assign rows to at most nbins equal-count bins once per fit, keeping equal feature values in the same bin. Tree nodes scan these global bins and refine the best coarse boundary over its two adjacent occupied bins. Nodes with at most nbins rows use exact search.\n\nThe sufficient statistics use the original feature values and responses, and weights retain their frequency meaning. This can change the selected model and predictions; it does not guarantee the exact optimum or an error bound. Use ExactSearch() or BinnedSearch() for categorical features, and ExactSearch() for vector-score losses. nbins must lie in 2:65535. Boosting reuses bins from all positive-weight training rows across rounds.\n\n\n\n\n\n","title":"LinearTrees.HybridSearch"},{"category":"type","location":"api/#LinearTrees.Loss","page":"API reference","text":"A twice-differentiable or IRLS-approximated loss. Implement gradhess!, linkinv, initscore, deviance, scorebound, and validate_target.\n\n\n\n\n\n","title":"LinearTrees.Loss"},{"category":"type","location":"api/#LinearTrees.MSE","page":"API reference","text":"Squared-error loss for a real-valued target. Identity link.\n\n\n\n\n\n","title":"LinearTrees.MSE"},{"category":"type","location":"api/#LinearTrees.Huber","page":"API reference","text":"Huber loss with transition δ: quadratic within δ of the target, linear beyond it. Identity link.\n\n\n\n\n\n","title":"LinearTrees.Huber"},{"category":"type","location":"api/#LinearTrees.Quantile","page":"API reference","text":"Pinball (quantile) loss at quantile τ. Non-smooth: fit by IRLS. Identity link.\n\n\n\n\n\n","title":"LinearTrees.Quantile"},{"category":"type","location":"api/#LinearTrees.MAD","page":"API reference","text":"Mean absolute deviation loss. Non-smooth: fit by IRLS. Identity link.\n\n\n\n\n\n","title":"LinearTrees.MAD"},{"category":"type","location":"api/#LinearTrees.Logistic","page":"API reference","text":"Logistic (binomial cross-entropy) loss for a {0,1} target. Logit link.\n\n\n\n\n\n","title":"LinearTrees.Logistic"},{"category":"type","location":"api/#LinearTrees.Poisson","page":"API reference","text":"Poisson deviance loss for a non-negative integer count target. Log link.\n\n\n\n\n\n","title":"LinearTrees.Poisson"},{"category":"type","location":"api/#LinearTrees.NegBin","page":"API reference","text":"Negative binomial deviance loss with dispersion θ, for an overdispersed count target. Log link.\n\n\n\n\n\n","title":"LinearTrees.NegBin"},{"category":"type","location":"api/#LinearTrees.Gamma","page":"API reference","text":"Gamma deviance loss for a positive real target. Log link.\n\n\n\n\n\n","title":"LinearTrees.Gamma"},{"category":"type","location":"api/#LinearTrees.Tweedie","page":"API reference","text":"Tweedie deviance loss with power ρ ∈ (1, 2), for a non-negative target with a point mass at zero. Log link.\n\n\n\n\n\n","title":"LinearTrees.Tweedie"},{"category":"type","location":"api/#LinearTrees.Softmax","page":"API reference","text":"Softmax(K)\n\nK-class softmax with class K as the reference at logit zero. Diagonal Hessian. K is a type parameter so the coefficient type SVector{K-1,T} is known at compile time and every fit specialises on it.\n\n\n\n\n\n","title":"LinearTrees.Softmax"},{"category":"type","location":"api/#LinearTrees.Frozen","page":"API reference","text":"Frozen{V}() <: Loss\n\nBoosting's per-tree loss. Each target row is a pair (z0, h0) of working response and frozen Hessian at the ensemble score, both of type V, so one tree minimises Σ w h0 (f − z0)² / 2: the second-order objective of a boosting round with z0 = −g0 / h0. gradhess! returns g = (f − z0) h0 and h = h0, including exact zero components, the start score is zero, the score bounds are infinite so truncate = true keeps only the feature clamp, and the loss is smooth so no IRLS refit runs. A tree fitted with Frozen predicts its raw score; only a LinearBoost gives it a link.\n\n\n\n\n\n","title":"LinearTrees.Frozen"},{"category":"function","location":"api/#LinearTrees.gradhess!","page":"API reference","text":"gradhess!(g, h, loss, y, f)\n\nUnweighted g = ∂ℓ/∂f and h = max(∂²ℓ/∂f², HMIN) per row. Frozen preserves its supplied Hessian, including zero. The caller applies frequency weights after any floor.\n\n\n\n\n\ngradhess!(g, h, ::Softmax, y, f)\n\nPer-row cross-entropy gradient and diagonal Hessian against the K-1 reference-class logits, with integer class labels y in 1:K.\n\n\n\n\n\n","title":"LinearTrees.gradhess!"},{"category":"function","location":"api/#LinearTrees.linkinv","page":"API reference","text":"linkinv(loss, s)\n\nMap raw score s to the response scale: the identity for MSE, Huber, Quantile, and MAD, the logistic sigmoid for Logistic, exp for the count and rate losses, and class probabilities for Softmax.\n\n\n\n\n\n","title":"LinearTrees.linkinv"},{"category":"function","location":"api/#LinearTrees.initscore","page":"API reference","text":"initscore(::Softmax, y, w)\n\nLog-odds of each non-reference class against the reference class K, from the weighted class frequencies.\n\n\n\n\n\n","title":"LinearTrees.initscore"},{"category":"function","location":"api/#StatsAPI.deviance","page":"API reference","text":"deviance(loss, y, f, w)\n\nWeighted 2 Σ w[i] pointloss(loss, y[i], f[i]), the model's reported deviance on the score scale f.\n\n\n\n\n\ndeviance(m::LinearTreeClassifierFit)\n\nTraining deviance against m's stored coded target: 0/1 (positive class classes[1]) for a two-class Logistic fit, the 1-based sorted-class index for a Softmax fit. score(m.tree, m.X) returns a plain Matrix for Softmax, so each row is repacked into the SVector pointloss needs.\n\n\n\n\n\n","title":"StatsAPI.deviance"},{"category":"function","location":"api/#LinearTrees.scorebound","page":"API reference","text":"scorebound(loss, y; truncation_factor=3)\n\n(lo, hi) score-scale clamp bounds fit to the training target y, used when fit_tree's truncate is set. With half-width B = (max(y) - min(y)) / 2, truncation_factor pads each side by (truncation_factor - 1) * B, so the bounds are [min(y) - (truncation_factor - 1) B, max(y) + (truncation_factor - 1) B].\n\n\n\n\n\n","title":"LinearTrees.scorebound"},{"category":"function","location":"api/#LinearTrees.validate_target","page":"API reference","text":"validate_target(loss, y)\n\nThrow ArgumentError if y is not finite everywhere or does not satisfy loss's domain (e.g. {0,1} for Logistic, non-negative integers for Poisson); otherwise return nothing.\n\n\n\n\n\n","title":"LinearTrees.validate_target"},{"category":"function","location":"api/#LinearTrees.issmooth","page":"API reference","text":"issmooth(loss)\n\ntrue when loss has a well-defined Hessian everywhere and fits by Newton steps; false for the L1-type losses (Quantile, MAD), which fit by IRLS.\n\n\n\n\n\n","title":"LinearTrees.issmooth"},{"category":"type","location":"api/#LinearTrees.AdaptedLoss","page":"API reference","text":"Adapter around a LossFunctions.SupervisedLoss. Margin losses receive targets in {0,1} and are re-expressed with t = 2y - 1. The pointwise loss, gradient, and hessian are multiplied by scale.\n\n\n\n\n\n","title":"LinearTrees.AdaptedLoss"},{"category":"type","location":"api/#LinearTrees.IdentityLink","page":"API reference","text":"Score-space link between the tree's raw score and the value the inner loss expects.\n\n\n\n\n\n","title":"LinearTrees.IdentityLink"},{"category":"type","location":"api/#LinearTrees.LogitLink","page":"API reference","text":"Score-space link marking the tree's raw score as a logit, for a margin loss.\n\n\n\n\n\n","title":"LinearTrees.LogitLink"},{"category":"type","location":"api/#LinearTrees.LogLink","page":"API reference","text":"Score-space link marking the tree's raw score as a log mean, for PoissonLoss.\n\n\n\n\n\n","title":"LinearTrees.LogLink"},{"category":"function","location":"api/#LinearTrees.canonical_scale","page":"API reference","text":"Newton-step scale that lines an inner loss up with the matching native Loss.\n\n\n\n\n\n","title":"LinearTrees.canonical_scale"},{"category":"function","location":"api/#LinearTrees.feature_importance","page":"API reference","text":"feature_importance(tree)\n\nSurrogate-deviance drop per feature, normalised to sum to one. Zeros when no node has positive gain.\n\n\n\n\n\nfeature_importance(boost)\n\nPositive split gains summed over every tree, normalised to sum to one.\n\n\n\n\n\n","title":"LinearTrees.feature_importance"},{"category":"function","location":"api/#StatsAPI.coeftable","page":"API reference","text":"coeftable(tree, x)\n\nIntercept and per-feature slopes of the unclipped score at x. A feature clamped at x contributes zero slope and its piece value goes to the intercept. Categorical pieces are constants.\n\n\n\n\n\ncoeftable(boost, x)\n\nIntercept and per-feature slopes of the unclipped ensemble score at x: f0 plus eta times the sum of each tree's coeftable.\n\n\n\n\n\n","title":"StatsAPI.coeftable"},{"category":"function","location":"api/#LinearTrees.shap","page":"API reference","text":"shap(tree, X; nthreads=Threads.nthreads()) -> ShapResult\n\nPath-dependent TreeSHAP values for every row of X against every feature of tree, on the unclipped score scale. Each row's SHAP values sum to score_row(tree, X, i, false) - result.base, the game's efficiency identity.\n\n\n\n\n\nshap(boost, X; nthreads=Threads.nthreads()) -> ShapResult\n\nPath-dependent TreeSHAP for a LinearBoost; see shap!.\n\n\n\n\n\n","title":"LinearTrees.shap"},{"category":"function","location":"api/#LinearTrees.shap!","page":"API reference","text":"shap!(values, clipped, tree, X; nthreads=Threads.nthreads())\n\nIn-place shap: write into values (n × p, or n × p × (K-1) for Softmax) and clipped (Vector{Bool}, length n), then return a ShapResult wrapping them.\n\n\n\n\n\nshap!(values, clipped, boost, X; nthreads=Threads.nthreads())\n\nIn-place shap for an ensemble: eta times the sum of every tree's path-dependent SHAP values, base f0 + eta Σ expected_score(tree), so each row sums to score(boost, x; clip = false) − base. clipped[i] is true when the ensemble clamp changed row i. One PathPool per row block, reused across the ensemble's trees.\n\n\n\n\n\n","title":"LinearTrees.shap!"},{"category":"type","location":"api/#LinearTrees.ShapResult","page":"API reference","text":"ShapResult{A,B,C}\n\nvalues is n × p for scalar-score trees, n × p × (K-1) for Softmax. base is the game's empty-coalition value, expected_score(tree); fit_tree stores the same number in tree.base. clipped[i] is true when the score bound changed row i's prediction.\n\n\n\n\n\n","title":"LinearTrees.ShapResult"},{"category":"function","location":"api/#LinearTrees.expected_score","page":"API reference","text":"expected_score(tree) -> V\n\nCover-weighted game_value(tree, ·, ∅) over the whole tree: at each split the absent feature's piece is evaluated at the node's xmean on both children, weighted by cover(child) / cover(parent). Independent of x. fit_tree stores it in tree.base. shap/shap! recompute it from the nodes (O(nodes)) so a hand-built tree whose base field is stale still satisfies the efficiency identity.\n\n\n\n\n\n","title":"LinearTrees.expected_score"},{"category":"type","location":"api/#LinearTrees.LinearTreeRegressorFit","page":"API reference","text":"LinearTreeRegressorFit{Tr}\n\nA fit_tree result wrapped with its input encoder and training data as a StatsAPI.RegressionModel. Build with fit(LinearTreeRegressorFit, X, y; loss=MSE(), weights=nothing, unseen=:error, kwargs...), where X is a matrix or a Tables.jl table and kwargs forward to fit_tree.\n\n\n\n\n\n","title":"LinearTrees.LinearTreeRegressorFit"},{"category":"type","location":"api/#LinearTrees.LinearTreeClassifierFit","page":"API reference","text":"LinearTreeClassifierFit{Tr,C}\n\nA fit_tree result for a categorical target, wrapped as a StatsAPI.StatisticalModel. Build with fit(LinearTreeClassifierFit, X, y; weights=nothing, unseen=:error, kwargs...). classes = sort(unique(y)) fixes both the label-to-code map and the column order of predict's probability matrix. Two classes fit a single Logistic tree with classes[1] (the first sorted class) as the positive outcome; predict returns hcat(p1, 1 .- p1), so column k is still P(classes[k]). Three or more classes fit a Softmax tree with classes[k] at coordinate k.\n\n\n\n\n\n","title":"LinearTrees.LinearTreeClassifierFit"},{"category":"type","location":"api/#LinearTrees.LinearBoostRegressorFit","page":"API reference","text":"LinearBoostRegressorFit{B}\n\nA fit_boost result wrapped with its input encoder and training data as a StatsAPI.RegressionModel. Build with fit(LinearBoostRegressorFit, X, y; loss=MSE(), weights=nothing, unseen=:error, Xval=nothing, yval=nothing, wval=nothing, kwargs...); Xval may be a matrix or table and is encoded like X; kwargs forward to fit_boost.\n\n\n\n\n\n","title":"LinearTrees.LinearBoostRegressorFit"},{"category":"type","location":"api/#LinearTrees.LinearBoostClassifierFit","page":"API reference","text":"LinearBoostClassifierFit{B,C}\n\nA fit_boost result for a categorical target, with the same class conventions as LinearTreeClassifierFit: classes[1] is the Logistic positive outcome for two classes, and column k of predict is P(classes[k]). Build with fit(LinearBoostClassifierFit, X, y; weights=nothing, unseen=:error, Xval=nothing, yval=nothing, wval=nothing, kwargs...).\n\n\n\n\n\n","title":"LinearTrees.LinearBoostClassifierFit"},{"category":"function","location":"api/#StatsAPI.fit","page":"API reference","text":"fit(LinearTreeRegressorFit, X, y; loss=MSE(), weights=nothing, unseen=:error, kwargs...)\nfit(LinearTreeClassifierFit, X, y; weights=nothing, unseen=:error, kwargs...)\n\nFit a LinearTreeRegressorFit or LinearTreeClassifierFit from a matrix or Tables.jl table X and a target y. kwargs forward to fit_tree. unseen is :error (default) or :right, applied to a categorical level absent from training at predict time.\n\n\n\n\n\n","title":"StatsAPI.fit"},{"category":"function","location":"api/#StatsAPI.nobs","page":"API reference","text":"Number of rows passed to fit, zero-weight rows included.\n\n\n\n\n\n","title":"StatsAPI.nobs"},{"category":"function","location":"api/#StatsAPI.dof","page":"API reference","text":"dof(m)\n\nCount of stored coefficients across every node: 1 for CON, 2 for LIN or PCON, 3 for BLIN, 4 for PLIN, times K-1 for a Softmax fit. A parameter count, not an effective degrees of freedom, and unrelated to BIC's per-kind selection penalty of the same name.\n\n\n\n\n\n","title":"StatsAPI.dof"},{"category":"function","location":"api/#StatsAPI.weights","page":"API reference","text":"Training row weights, as stored (ones when fit was called with no weights).\n\n\n\n\n\n","title":"StatsAPI.weights"},{"category":"function","location":"api/#StatsAPI.residuals","page":"API reference","text":"Training residuals y - predict(tree, X), on the response scale.\n\n\n\n\n\n","title":"StatsAPI.residuals"},{"category":"type","location":"api/#LinearTrees.LinearTreeRegressor","page":"API reference","text":"LinearTreeRegressor\n\nA model type for constructing a linear tree regressor, based on LinearTrees.jl, and implementing the MLJ model interface.\n\nFrom MLJ, the type can be imported using\n\nLinearTreeRegressor = @load LinearTreeRegressor pkg=LinearTrees\n\nDo model = LinearTreeRegressor() to construct an instance with default hyper-parameters. Provide keyword arguments to override hyper-parameter defaults, as in LinearTreeRegressor(loss=...).\n\nLinearTreeRegressor fits a PILOT-style linear model tree for a continuous or count target, generalised to any twice-differentiable loss.\n\nHyperparameters\n\nloss::Loss = MSE(): the loss to fit; see MSE, Huber, Quantile, MAD, Poisson, NegBin, Gamma, Tweedie.\nrule::SelectionRule = BIC(): model-kind selection rule at each node.\nmax_depth::Int = 12: maximum tree depth.\nmin_samples_split::Int = 10: minimum total weight in a node to attempt a split.\nmin_samples_leaf::Int = 5: minimum total weight required in each child.\nmin_sum_hessian::Float64 = 1.0: minimum summed hessian in a node to attempt a split.\nmax_lin_chain::Int = 10: maximum consecutive lin fits along one root-to-node path.\ntruncate::Bool = true: clamp predictions to the training score range.\ntruncation_factor::Float64 = 3.0: half-range multiplier for the truncation bounds.\n\nOperations\n\npredict(mach, Xnew): return the predicted response for Xnew.\n\nFitted parameters\n\nThe fields of fitted_params(mach) are:\n\ntree: the fitted LinearTree.\n\nReport\n\nThe fields of report(mach) are:\n\nfeature_importances: split-gain feature importances, as in feature_importance.\nkind_counts: a count of fitted nodes by ModelKind.\n\nSee also LinearTreeClassifier.\n\n\n\n\n\n","title":"LinearTrees.LinearTreeRegressor"},{"category":"type","location":"api/#LinearTrees.LinearTreeClassifier","page":"API reference","text":"LinearTreeClassifier\n\nA model type for constructing a linear tree classifier, based on LinearTrees.jl, and implementing the MLJ model interface.\n\nFrom MLJ, the type can be imported using\n\nLinearTreeClassifier = @load LinearTreeClassifier pkg=LinearTrees\n\nDo model = LinearTreeClassifier() to construct an instance with default hyper-parameters. Provide keyword arguments to override hyper-parameter defaults, as in LinearTreeClassifier(rule=...).\n\nLinearTreeClassifier fits a PILOT-style linear model tree for a finite (two- or many-class) target: a Logistic tree for two classes, a Softmax tree for three or more.\n\nHyperparameters\n\nrule::SelectionRule = BIC(): model-kind selection rule at each node.\nmax_depth::Int = 12: maximum tree depth.\nmin_samples_split::Int = 10: minimum total weight in a node to attempt a split.\nmin_samples_leaf::Int = 5: minimum total weight required in each child.\nmin_sum_hessian::Float64 = 1.0: minimum summed hessian in a node to attempt a split.\nmax_lin_chain::Int = 10: maximum consecutive lin fits along one root-to-node path.\ntruncate::Bool = true: clamp predictions to the training score range.\ntruncation_factor::Float64 = 3.0: half-range multiplier for the truncation bounds.\n\nOperations\n\npredict(mach, Xnew): return the predicted class distribution for Xnew, as a UnivariateFinite. Use predict_mode(mach, Xnew) for point predictions.\n\nFitted parameters\n\nThe fields of fitted_params(mach) are:\n\ntree: the fitted LinearTree.\n\nReport\n\nThe fields of report(mach) are:\n\nfeature_importances: split-gain feature importances, as in feature_importance.\nkind_counts: a count of fitted nodes by ModelKind.\n\nSee also LinearTreeRegressor.\n\n\n\n\n\n","title":"LinearTrees.LinearTreeClassifier"},{"category":"type","location":"api/#LinearTrees.LinearBoostRegressor","page":"API reference","text":"LinearBoostRegressor\n\nA model type for constructing a linear boost regressor, based on LinearTrees.jl, and implementing the MLJ model interface.\n\nFrom MLJ, the type can be imported using\n\nLinearBoostRegressor = @load LinearBoostRegressor pkg=LinearTrees\n\nDo model = LinearBoostRegressor() to construct an instance with default hyper-parameters. Provide keyword arguments to override hyper-parameter defaults, as in LinearBoostRegressor(loss=...).\n\nLinearBoostRegressor fits a gradient-boosted ensemble of linear model trees for a continuous or count target. MLJ iteration controls nrounds; this model does not expose validation early stopping, for which use IteratedModel.\n\nHyperparameters\n\nloss::Loss = MSE(): scalar-response loss, such as MSE, Huber, Quantile, MAD, Poisson, NegBin, Gamma, or Tweedie.\nnrounds::Int = 100: number of boosting rounds.\neta::Float64 = 0.1: shrinkage per round.\nmax_depth::Int = 5: maximum base-tree depth.\nmin_samples_split::Int = 10: minimum node weight for a split.\nmin_samples_leaf::Int = 5: minimum child weight.\nmin_sum_hessian::Float64 = 1.0: minimum node Hessian sum.\nlambda_slope::Float64 = 1.0: slope penalty.\nlambda_intercept::Float64 = 1.0: intercept penalty.\ngamma::Float64 = 0.0: split-gain penalty.\nsubsample::Float64 = 1.0: row-sampling fraction.\ncolsample::Float64 = 1.0: feature-sampling fraction.\ntruncate::Bool = true: clamp predictions to the training score range.\nrng::Union{AbstractRNG,Integer} = default_rng(): random source; an integer starts a fresh Xoshiro on each fit.\n\nFitted parameters and report\n\nfitted_params(mach).boost is the fitted LinearBoost. report(mach) holds feature_importances, nrounds, and per-round history; the history is also available through training_losses(mach).\n\nSee also LinearBoostClassifier.\n\n\n\n\n\n","title":"LinearTrees.LinearBoostRegressor"},{"category":"type","location":"api/#LinearTrees.LinearBoostClassifier","page":"API reference","text":"LinearBoostClassifier\n\nA model type for constructing a linear boost classifier, based on LinearTrees.jl, and implementing the MLJ model interface.\n\nFrom MLJ, the type can be imported using\n\nLinearBoostClassifier = @load LinearBoostClassifier pkg=LinearTrees\n\nDo model = LinearBoostClassifier() to construct an instance with default hyper-parameters. Provide keyword arguments to override hyper-parameter defaults, as in LinearBoostClassifier(split_search=...).\n\nLinearBoostClassifier fits a gradient-boosted ensemble of linear model trees for a finite target. It uses Logistic loss for two classes and Softmax for three or more. MLJ iteration controls nrounds; this model does not expose validation early stopping, for which use IteratedModel.\n\nHyperparameters\n\nnrounds::Int = 100: number of boosting rounds.\neta::Float64 = 0.1: shrinkage per round.\nmax_depth::Int = 5: maximum base-tree depth.\nmin_samples_split::Int = 10: minimum node weight for a split.\nmin_samples_leaf::Int = 5: minimum child weight.\nmin_sum_hessian::Float64 = 1.0: minimum node Hessian sum.\nlambda_slope::Float64 = 1.0: slope penalty.\nlambda_intercept::Float64 = 1.0: intercept penalty.\ngamma::Float64 = 0.0: split-gain penalty.\nsubsample::Float64 = 1.0: row-sampling fraction.\ncolsample::Float64 = 1.0: feature-sampling fraction.\ntruncate::Bool = true: clamp predictions to the training score range.\nrng::Union{AbstractRNG,Integer} = default_rng(): random source; an integer starts a fresh Xoshiro on each fit.\n\nOperations\n\npredict(mach, Xnew) returns a UnivariateFinite; use predict_mode for point predictions.\n\nFitted parameters and report\n\nfitted_params(mach).boost is the fitted LinearBoost. report(mach) holds feature_importances, nrounds, and per-round history; the history is also available through training_losses(mach).\n\nSee also LinearBoostRegressor.\n\n\n\n\n\n","title":"LinearTrees.LinearBoostClassifier"},{"category":"type","location":"api/#LinearTrees.TreeView","page":"API reference","text":"A named view of a tree for AbstractTrees traversal and printing.\n\n\n\n\n\n","title":"LinearTrees.TreeView"},{"category":"function","location":"api/#LinearTrees.to_dict","page":"API reference","text":"to_dict(tree)\n\nJSON-friendly form: nodes as vectors of field dictionaries, loss as a name plus parameters. Three encodings keep every field JSON-safe:\n\nEvery Real-valued field (threshold, lcoef, lintercept, rcoef, rintercept, xmin, xmax, cover, xmean, gain, lo, hi, base) goes through jsonnum: finite values become Float64, non-finite values become the string \"Inf\", \"-Inf\", or \"NaN\", and an SVector field maps elementwise into a Vector{Any}.\nInteger fields (feature, left, right, catstart, catwords, model) store as Int, restored to their struct field type (Int32 or ModelKind) by from_dict.\ncatmasks (UInt64, may use the top bit) store as lowercase hex strings via string(m; base = 16), since a plain integer would overflow Int64 and lose exactness in a JSON parser; restored with parse(UInt64, s; base = 16).\n\n\n\n\n\n","title":"LinearTrees.to_dict"},{"category":"function","location":"api/#LinearTrees.from_dict","page":"API reference","text":"from_dict(d) -> LinearTree\n\nInverse of to_dict. Throws ArgumentError for an unrecognised format version, element type, or loss name.\n\n\n\n\n\n","title":"LinearTrees.from_dict"},{"category":"section","location":"interfaces/#Tables-and-model-interfaces","page":"Interfaces and persistence","text":"Use the direct API when data is already a numeric matrix. Use StatsAPI wrappers for Tables.jl inputs, stored preprocessing, and standard model statistics. Use the MLJ models inside an MLJ workflow.","title":"Tables and model interfaces"},{"category":"section","location":"interfaces/#StatsAPI-regression","page":"Interfaces and persistence","text":"LinearTreeRegressorFit stores the tree, encoded training matrix, response, weights, and table encoder. Keywords after X and y pass through to fit_tree.\n\nusing LinearTrees\n\nX = (temperature = collect(range(8.0, 25.0; length = 60)),\n     weekend = repeat([0, 1]; outer = 30))\ny = 0.4 .* X.temperature .+ 2 .* X.weekend\n\nfit = LinearTrees.fit(LinearTreeRegressorFit, X, y; max_depth = 1)\nŷ = LinearTrees.predict(fit, X)\n(nobs(fit), dof(fit), maximum(abs, residuals(fit)), deviance(fit))\n(60, 6, 0.04834676298971097, 0.0499722029235044)\n\nPass weights=... for observation weights. weights returns the stored weights. nobs counts all supplied rows, including zero-weight rows. dof counts stored coefficients. It is not an effective degrees-of-freedom estimate.","title":"StatsAPI regression"},{"category":"section","location":"interfaces/#StatsAPI-classification","page":"Interfaces and persistence","text":"LinearTreeClassifierFit accepts arbitrary sortable labels. It fits Logistic for two observed classes and Softmax otherwise. Predictions form an n × K probability matrix. The columns follow fit.classes.\n\nX = (x1 = repeat([-2.0, -1.0, 1.0, 2.0, 0.0, 0.5]; outer = 10),\n     x2 = repeat([0.0, 1.0, 0.0, 1.0, 2.0, 2.5]; outer = 10))\ny = repeat([\"low\", \"low\", \"high\", \"high\", \"middle\", \"middle\"]; outer = 10)\n\nfit = LinearTrees.fit(LinearTreeClassifierFit, X, y; max_depth = 1)\nP = LinearTrees.predict(fit, X)\n(fit.classes, size(P), P[1:3, :])\n([\"high\", \"low\", \"middle\"], (60, 3), [0.0014471983738204924 0.9784729347440237 0.020079866882155736; 0.013235019033864606 0.8031291883959575 0.18363579257017792; 0.7081068466614899 0.022701274776530753 0.26919187856197935])\n\nFor a binary fit, fit.classes[1] is the positive class. The first probability column is its probability.\n\nThe boosting wrappers are LinearBoostRegressorFit and LinearBoostClassifierFit. They follow the same table and class conventions. See Boosting.","title":"StatsAPI classification"},{"category":"section","location":"interfaces/#Table-schema-and-categorical-columns","page":"Interfaces and persistence","text":"The wrappers accept matrices and Tables.jl tables, including named tuples and DataFrames. Table column names must match the fitted encoder during prediction. Columns are retrieved by name, so their order may change. Numeric values convert to Float64. Missing, non-finite, and non-numeric numeric-column values are unsupported.\n\nA column is categorical when it provides a DataAPI.refpool, as CategoricalArrays does. Training levels are sorted by their string form and stored with the model. Prediction uses this stored map. Changing a categorical pool's internal order does not change its encoding.\n\nThe default unseen=:error rejects a categorical level absent during training. Set unseen=:right at fit time to route unseen levels right at categorical splits.\n\nusing LinearTrees\nusing CategoricalArrays\nusing DataFrames\n\nX = DataFrame(colour = categorical(repeat([\"red\", \"blue\", \"green\"]; outer = 20)),\n              size = collect(range(1.0, 4.0; length = 60)))\ny = repeat([1.0, 2.0, 3.0]; outer = 20) .+ 0.2 .* X.size\n\nfit = LinearTrees.fit(LinearTreeRegressorFit, X, y; unseen = :right)\nXnew = DataFrame(colour = categorical([\"purple\"]), size = [2.5])\nLinearTrees.predict(fit, Xnew)\n\nThis example needs CategoricalArrays.jl and DataFrames.jl.","title":"Table schema and categorical columns"},{"category":"section","location":"interfaces/#MLJ","page":"Interfaces and persistence","text":"LinearTrees provides four MLJ models:\n\nLinearTreeRegressor\nLinearTreeClassifier\nLinearBoostRegressor\nLinearBoostClassifier\n\nConstruct the model directly, then use the normal MLJ machine workflow.\n\nusing LinearTrees\nusing MLJBase\n\nX = (x1 = collect(1.0:60.0), x2 = repeat([0, 1]; outer = 30))\ny = 2 .* X.x1 .+ X.x2\n\nmodel = LinearTreeRegressor(max_depth = 2)\nmach = machine(model, X, y)\nfit!(mach)\nŷ = MLJBase.predict(mach, X)\n\nThis example needs MLJBase.jl, which provides the machine interface. The full MLJ.jl package re-exports this workflow and adds other learning tools. Qualify prediction as MLJBase.predict here, or MLJ.predict when using MLJ.jl. LinearTrees also exports predict for directly fitted trees and wrappers.\n\nClassifier machines return MLJ UnivariateFinite distributions over the training levels. MLJ reports feature importances for all four models. Boosting models also report training loss history. They expose nrounds as their iteration parameter.","title":"MLJ"},{"category":"section","location":"interfaces/#Persistence","page":"Interfaces and persistence","text":"Use JLD2 for Julia-native persistence of trees, fitted wrappers, and boosting models. It preserves the complete Julia object graph.\n\nusing JLD2\nusing LinearTrees\n\nJLD2.jldsave(\"model.jld2\"; model = fit)\nfit = JLD2.load(\"model.jld2\", \"model\")\n\nThis example needs JLD2.jl. JLD2 is optional and is not installed with LinearTrees.\n\nUse to_dict and from_dict for one LinearTree. The returned dictionary is compatible with JSON encoders.\n\nX = reshape(collect(1.0:60.0), :, 1)\ny = 2 .* vec(X)\ntree = fit_tree(X, y; max_depth = 1)\nrestored = from_dict(to_dict(tree))\nLinearTrees.predict(restored, X) == LinearTrees.predict(tree, X)\ntrue\n\nThe dictionary stores nodes, categorical masks, loss, score bounds, base value, feature count, and truncation. It supports Float32 and Float64 trees with built-in losses. It does not serialize StatsAPI wrappers, MLJ machines, boosting models, or AdaptedLoss. Use a JSON package to encode the dictionary when text interchange is needed.","title":"Persistence"},{"category":"section","location":"interpretation/#Interpreting-models","page":"Interpretation","text":"LinearTrees provides three complementary views of a fitted tree or ensemble. feature_importance summarizes split gains across the model. coeftable describes the linear score near one observation. shap attributes one prediction across features.\n\nAll three methods describe the fitted model. They do not estimate causal effects.","title":"Interpreting models"},{"category":"section","location":"interpretation/#Rank-features-by-split-gain","page":"Interpretation","text":"feature_importance sums each feature's positive split gains. It then normalizes the values to sum to one. It returns all zeros when the model has no positive split gain.\n\nusing LinearTrees\n\nx1 = collect(range(-1, 1; length = 80))\nx2 = reverse(x1)\nX = [x1 x2]\ny = 3 .* x1 .+ 0.2 .* x2 .^ 2\ntree = fit_tree(X, y; max_depth = 3)\n\nfeature_importance(tree)\n2-element Vector{Float64}:\n 3.7838117351053475e-5\n 0.999962161882649\n\nFor an ensemble, gains are summed across all retained trees before normalization. Gain importance can favor features offering many useful split points. Correlated features can divide or exchange importance. The result gives no direction for a feature's effect.","title":"Rank features by split gain"},{"category":"section","location":"interpretation/#Inspect-the-local-linear-score","page":"Interpretation","text":"coeftable returns (intercept, slopes) at one feature vector x. The selected path determines the returned linear piece. For scalar scores, intercept + sum(slopes .* x) equals the unclipped score.\n\nx = X[20, :]\nintercept, slopes = coeftable(tree, x)\nlocal_score = intercept + sum(slopes .* x)\nraw_score = only(score(tree, reshape(x, 1, :); clip = false))\n\n(intercept, slopes, local_score, raw_score)\n(-0.08168849710747343, [0.042494114550282555, -2.700184895462947], -1.5051041858485166, -1.5051041858485166)\n\nCategorical features contribute constants and therefore have zero slopes. With truncation enabled, an out-of-range numeric feature can also have zero slope. Its clamped contribution moves into the intercept.\n\nThe coefficients describe only the path selected at x. Crossing a split can change the intercept, slope, or both. The coefficients remain on the score scale. Apply the loss link before comparing them with response-scale predictions.\n\nFor Softmax, the intercept and each slope are vectors with K - 1 coordinates. Their sum reconstructs the corresponding unclipped score vector.","title":"Inspect the local linear score"},{"category":"section","location":"interpretation/#Explain-predictions-with-SHAP-values","page":"Interpretation","text":"shap returns a ShapResult with values, base, and clipped. For scalar-score models, values has shape n × p. Each row reconstructs its raw score by adding base and its feature values.\n\nresult = shap(tree, X[1:4, :])\nreconstructed = result.base .+ vec(sum(result.values; dims = 2))\nraw = score(tree, X[1:4, :]; clip = false)\n\n(size(result.values), result.base, reconstructed, raw, result.clipped)\n((4, 2), -0.06609549123375552, [-2.8011507914680718, -2.734260439510507, -2.6673700875529414, -2.600479735595377], [-2.8011507914680718, -2.7342604395105066, -2.6673700875529414, -2.600479735595377], Bool[0, 0, 0, 0])\n\nFor Softmax(K), values has shape n × p × (K - 1). base has K - 1 coordinates. Each final-axis slice explains one score coordinate. predict converts those scores into K response probabilities.\n\nusing LinearTrees\n\nx1 = repeat([-1.0, 0.0, 1.0], 30)\nx2 = repeat(collect(range(-1, 1; length = 30)), inner = 3)\nX = [x1 x2]\ny = [a > 0 ? 1 : b > 0 ? 2 : 3 for (a, b) in zip(x1, x2)]\ntree = fit_tree(X, y, Softmax(3); max_depth = 2)\nresult = shap(tree, X[1:5, :])\n\n(size(result.values), length(result.base), size(score(tree, X[1:5, :])), size(predict(tree, X[1:5, :])))\n((5, 2, 2), 2, (5, 2), (5, 3))\n\nThese are path-dependent TreeSHAP values. Node cover fractions weight branches when a feature is absent. They do not integrate over an independent background dataset. Correlated features can therefore receive attribution according to the learned paths.","title":"Explain predictions with SHAP values"},{"category":"section","location":"interpretation/#Account-for-clipping","page":"Interpretation","text":"SHAP and local coefficients reconstruct the unclipped score. They do not reconstruct a clipped score or linked response directly. result.clipped[i] is true when score truncation changed row i.\n\nCompare these quantities when exact reconstruction matters:\n\nraw = score(model, X; clip = false)\nbounded = score(model, X)\nresponse = predict(model, X)\nexplanation = shap(model, X)\n\nWhen explanation.clipped[i] is true, the SHAP sum still equals raw[i]. The bounded score and response no longer equal that additive decomposition. Use truncate = false during fitting only when unbounded behavior suits the application.\n\nUse shap! when reusing preallocated output arrays matters. See Boosted trees for ensemble fitting and retained rounds.","title":"Account for clipping"},{"category":"section","location":"performance/#Performance","page":"Performance","text":"Fit quality, runtime, and memory use depend on the search method and the data. Measure complete fits on representative inputs and compare quality on held-out rows before changing the search policy.","title":"Performance"},{"category":"section","location":"performance/#Choose-a-split-search-method","page":"Performance","text":"fit_tree and fit_boost accept the same split_search keyword:\n\nMethod Numeric thresholds Categorical features Softmax\nExactSearch All eligible thresholds in each node Supported Supported\nBinnedSearch Node-local bins, with optional refinement Exact categorical search Unsupported\nHybridSearch Global bins, then local refinement Unsupported Unsupported\n\nUse exact search as the reference. Try local or hybrid search for scalar-score fits where threshold search is expensive. Even Softmax(2) uses vector scores and requires exact search.\n\nusing LinearTrees, Random, Statistics\n\nrng = Xoshiro(8)\nX = rand(rng, 600, 4)\ny = sin.(6 .* X[:, 1]) .+ X[:, 2] .+ 0.1 .* randn(rng, 600)\ntrain, valid = 1:450, 451:600\nsearches = (ExactSearch(), BinnedSearch(nbins = 64), HybridSearch(nbins = 64))\n\nmap(searches) do search\n    tree = fit_tree(X[train, :], y[train]; split_search = search, max_depth = 4)\n    error = predict(tree, X[valid, :]) .- y[valid]\n    (search = nameof(typeof(search)), rmse = round(sqrt(mean(abs2, error)); digits = 3))\nend\n((search = :ExactSearch, rmse = 0.105), (search = :BinnedSearch, rmse = 0.105), (search = :HybridSearch, rmse = 0.105))","title":"Choose a split-search method"},{"category":"section","location":"performance/#Local-bins","page":"Performance","text":"BinnedSearch(nbins = 64, refine = true) builds bins from each node's rows. Tied values stay together. Candidate scores use weighted moments of the original values, so binning restricts thresholds without replacing features by bin centers.\n\nRefinement searches the two bins beside the winning coarse boundary. It does not run when a constant or unsplit line wins. Small nodes use exact search. Set refine = false to omit refinement.","title":"Local bins"},{"category":"section","location":"performance/#Global-bins-with-local-refinement","page":"Performance","text":"HybridSearch(nbins = 64) prepares bins from the training data once and reuses them as the tree grows. It refines the winning boundary using raw rows in its neighboring occupied bins. Small nodes use exact search.\n\nFor boosting, bins come from all positive-weight training rows and are reused across rounds. Row sampling selects from those bins. Validation data never determines bin boundaries.\n\nHybrid search avoids the full presort index used by exact and local-bin search. This can reduce memory use, but need not give the fastest fit. Its bin count must lie in 2:65535.\n\nBoth approximate methods can change node choices and predictions. Neither has an approximation-error bound. More bins allow more thresholds, but held-out accuracy need not improve monotonically.","title":"Global bins with local refinement"},{"category":"section","location":"performance/#Threads","page":"Performance","text":"Start Julia with multiple threads, then set how many a call may use:\n\njulia --threads=auto --project\n\ntree = fit_tree(X, y; nthreads = 4)\nyhat = predict(tree, Xnew; nthreads = 4)\n\nThe default is Threads.nthreads(). Small workloads remain serial where threading would add overhead. More threads can increase scratch-memory use. Use nthreads = 1 when an outer loop already parallelizes independent fits.","title":"Threads"},{"category":"section","location":"performance/#Reuse-prediction-storage","page":"Performance","text":"predict! fills a caller-provided output array:\n\ntree = fit_tree(X[train, :], y[train]; max_depth = 4)\nXnew = X[valid, :]\nout = Vector{Float64}(undef, size(Xnew, 1))\npredict!(out, tree, Xnew)\nround.(out[1:4]; digits = 3)\n4-element Vector{Float64}:\n  1.383\n -0.771\n  1.561\n -0.314\n\nFor Softmax(K), use an n × K matrix. Storage reuse does not change the model or prediction scale.","title":"Reuse prediction storage"},{"category":"section","location":"performance/#Measure-repeatable-work","page":"Performance","text":"Separate compilation and data construction from timed fitting. BenchmarkTools is an optional benchmarking dependency:\n\nusing BenchmarkTools\n\nfit_tree(X, y; nthreads = 1) # compile before measuring\n@benchmark fit_tree($X, $y; nthreads = 1)\n\nInspect allocation counts and bytes as well as runtime. For boosted fits with sampling, create a fresh seeded RNG for each repetition. Reusing an advanced RNG changes the sampled trees between calls.","title":"Measure repeatable work"},{"category":"section","location":"losses/#Choosing-a-loss","page":"Loss functions","text":"A loss defines the target domain, fitting scale, and transformation applied by predict. Pass the loss as the third argument to fit_tree:\n\nusing LinearTrees\n\nX = reshape(collect(0.0:5.0), :, 1)\ny = [0, 0, 1, 2, 3, 5]\ntree = fit_tree(X, y, Poisson(); max_depth = 1)\n(score = score(tree, X), response = predict(tree, X))\n(score = [0.6061358035703156, 0.6061358035703156, 0.6061358035703156, 0.6061358035703156, 0.6061358035703156, 0.6061358035703156], response = [1.8333333333333335, 1.8333333333333335, 1.8333333333333335, 1.8333333333333335, 1.8333333333333335, 1.8333333333333335])\n\nscore returns values on the loss scale, with score clipping enabled by default. predict applies the inverse link. They are equal for identity-link losses. Logistic scores are log odds. Log-link scores are log means. Softmax scores are class logits.\n\nChoose a loss from the target and the quantity you want to estimate.\n\nTarget Loss Prediction\nReal-valued response MSE Conditional mean\nReal-valued response with outliers Huber(δ) Robust location\nReal-valued response Quantile(τ) Conditional quantile\nReal-valued response MAD Conditional median\nBinary value in {0, 1} Logistic Probability of 1\nNon-negative integer count Poisson Conditional mean\nOverdispersed non-negative integer count NegBin(θ) Conditional mean\nPositive continuous response Gamma Conditional mean\nNon-negative semicontinuous response Tweedie(ρ) Conditional mean\nInteger class code in 1:K Softmax(K) Class probabilities\n\nHuber(δ) changes from squared to linear loss at residual magnitude δ. Quantile(τ) requires 0 < τ < 1. NegBin(θ) requires θ > 0. Tweedie(ρ) supports powers 1 < ρ < 2.\n\nvalidate_target checks finite values and each loss domain. Every class from 1 through K must occur when fitting Softmax(K).","title":"Choosing a loss"},{"category":"section","location":"losses/#Classification-conventions","page":"Loss functions","text":"Use numeric targets in {0, 1} with Logistic. A prediction is the probability that the target equals 1.\n\nThe Tables and StatsAPI classifier accepts labels. It sorts observed labels and stores that order in fit.classes. For two classes, fit.classes[1] is the positive class. Probability column k always represents fit.classes[k].\n\nSoftmax(K) uses class K as the reference class. score returns K - 1 logits against that reference. predict returns all K probabilities in class-code order.\n\nX = reshape([-2.0, -1.0, 1.0, 2.0, 0.0, 0.5], :, 1)\ny = [1, 1, 2, 2, 3, 3]\ntree = fit_tree(X, y, Softmax(3); max_depth = 1)\n(size(score(tree, X)), size(predict(tree, X)))\n((6, 2), (6, 3))\n\nSoftmax fitting uses the diagonal of the reference-logit Hessian. It omits cross-class Hessian terms. For Softmax(2), this diagonal is the exact logistic Hessian.","title":"Classification conventions"},{"category":"section","location":"losses/#How-losses-are-fitted","page":"Loss functions","text":"Smooth losses fit each candidate model from gradients and Hessians on the raw score scale. The working response is -g/h, and the working weight is h. The method takes one local Newton step. It does not solve every node's full nonlinear optimization problem.\n\nThe main derivatives are:\n\nLoss Gradient g Hessian h\nMSE f - y 1\nLogistic p - y p(1-p)\nPoisson μ - y μ\n\nHere f is the score, p is the logistic inverse link, and μ = exp(f). gradhess! provides the complete loss-specific definitions. Ordinary row Hessians have a floor of 1e-6 before sample weights apply.\n\nQuantile and MAD are non-smooth. They use iteratively reweighted least squares instead of a true Newton step. Split search computes one set of residual weights. After selection, the chosen node refits for niter iterations, which defaults to five. The result approximates the node's L1 objective.\n\nBoosting freezes the non-smooth working weights once per round. Its base tree does not run the node refit. See Boosting for the ensemble algorithm.","title":"How losses are fitted"},{"category":"section","location":"losses/#Logistic-step-safeguard","page":"Loss functions","text":"Small logistic Hessians can produce an excessive Newton step. Logistic trees and binary softmax trees compare each node update with the current weighted training loss. They keep a non-increasing full step. Otherwise, they halve all node coefficients together until the loss does not increase. They use a zero update if no tested scale succeeds.\n\nThe check includes score and feature truncation. It ensures descent for that node update within floating-point accuracy. It does not find the exact maximum-likelihood node fit or calibrate held-out probabilities.","title":"Logistic step safeguard"},{"category":"section","location":"losses/#Deviance-and-truncation","page":"Loss functions","text":"deviance reports\n\n2 sum_i w_iell(y_i f_i)\n\nwhere f is the raw score. This convention includes constants present in the implemented point loss. It omits distribution constants absent from that loss. Compare deviances fitted with the same loss and data. Lower values indicate a better fit under that loss.\n\nWith truncate=true, scorebound limits fitted scores. Identity-link losses derive bounds from the target range. Logistic and softmax use [-10, 10]. Log-link losses use symmetric bounds based on the largest target. Set truncate=false when extrapolation beyond the training range is required.","title":"Deviance and truncation"},{"category":"section","location":"losses/#LossFunctions.jl-losses","page":"Loss functions","text":"Wrap a supported LossFunctions.jl loss with Loss. Use IdentityLink for distance losses. Use LogitLink for margin losses. Use LogLink for PoissonLoss. canonical_scale matches an equivalent built-in loss where one exists.\n\nusing LinearTrees\nusing LossFunctions\n\nloss = Loss(LossFunctions.L2DistLoss(), IdentityLink())\ntree = fit_tree(X, y, loss)\n\nAdd LossFunctions.jl to your project to use its loss types directly. Adapted L1 and quantile losses use the same IRLS path as MAD and Quantile. Adapted LogitMarginLoss values use the logistic step safeguard.","title":"LossFunctions.jl losses"},{"category":"section","location":"boosting/#Boosted-trees","page":"Boosting","text":"fit_boost builds an additive ensemble of linear model trees. Each round fits one tree to the current loss gradient and Hessian. The learning rate eta scales that tree before adding it to the ensemble.\n\nUse boosting when one tree cannot capture enough structure. Start with shallow trees and a small learning rate. Use separate validation data to choose the retained number of rounds.","title":"Boosted trees"},{"category":"section","location":"boosting/#Fit-an-ensemble","page":"Boosting","text":"The matrix interface accepts observations in rows and features in columns. Choose the loss explicitly when the default MSE does not match the target.\n\nusing LinearTrees\nusing Random\n\nrng = Xoshiro(42)\nX = rand(rng, 160, 3)\ny = sin.(3 .* X[:, 1]) .+ X[:, 2] .* X[:, 3] .+ 0.05 .* randn(rng, 160)\n\nmodel = fit_boost(X, y;\n    nrounds = 40,\n    eta = 0.1,\n    max_depth = 3,\n    rng = Xoshiro(7),\n)\n\nyhat = predict(model, X)\n(nrounds(model), model.history[end], yhat[1:3])\n(40, 0.33282755637186623, [0.5843051499875667, 1.1501811757561904, 1.118299819651477])\n\npredict returns values on the response scale. score returns values on the loss score scale. These scales match for identity-link losses such as MSE. They differ for losses such as Logistic, Poisson, and Softmax.","title":"Fit an ensemble"},{"category":"section","location":"boosting/#Use-held-out-validation","page":"Boosting","text":"Pass Xval and yval together. Pass wval when validation observations have weights. The fitter records validation deviance after each round. It stops after patience rounds without improvement. It then retains the trees through the best validation round.\n\nusing LinearTrees\nusing Random\n\nrng = Xoshiro(19)\nX = rand(rng, 180, 3)\ny = 2 .* X[:, 1] .+ sin.(5 .* X[:, 2]) .+ 0.2 .* randn(rng, 180)\n\ntrain = 1:140\nvalid = 141:180\nmodel = fit_boost(X[train, :], y[train];\n    Xval = X[valid, :],\n    yval = y[valid],\n    nrounds = 100,\n    patience = 8,\n    eta = 0.1,\n    max_depth = 3,\n)\n\n(model.validated, nrounds(model), length(model.history))\n(true, 43, 43)\n\nmodel.history[t] contains the retained validation deviance after round t. Without validation data, it contains training deviance for every fitted round. Early stopping only runs when validation data is present. nrounds reports the retained tree count.\n\nKeep validation rows independent from fitting rows. Repeated tuning against one validation set can still overfit that set.","title":"Use held-out validation"},{"category":"section","location":"boosting/#Control-the-ensemble","page":"Boosting","text":"nrounds sets the maximum tree count. eta sets every tree's contribution. Smaller eta usually needs more rounds. max_depth, min_fit, min_leaf, and min_sum_hessian limit tree growth.\n\nEach base tree uses GainRule. lambda_slope penalizes fitted slopes. lambda_intercept penalizes fitted intercept updates. gamma requires more gain before accepting a split. All three values must be finite and nonnegative.\n\nsubsample selects a fraction of training rows for each tree. colsample selects a fraction of features for each tree. Both values lie in (0, 1]. Sampling occurs without replacement. Sampled-out rows still receive the fitted tree's update.\n\nPass a freshly seeded AbstractRNG to reproduce a sampled fit. The fitter advances the supplied generator. Thread count does not change a fit made from the same random stream.\n\nusing LinearTrees\nusing Random\n\nrng = Xoshiro(31)\nX = rand(rng, 120, 5)\ny = X[:, 1] .- 2 .* X[:, 3] .+ 0.1 .* randn(rng, 120)\n\nmodel = fit_boost(X, y;\n    nrounds = 25,\n    eta = 0.05,\n    max_depth = 2,\n    lambda_slope = 2.0,\n    lambda_intercept = 1.0,\n    gamma = 0.1,\n    subsample = 0.8,\n    colsample = 0.6,\n    rng = Xoshiro(11),\n)\n\nnrounds(model)\n25\n\nChoose a split search as described in Performance. Approximate searches have extra loss and feature restrictions.","title":"Control the ensemble"},{"category":"section","location":"boosting/#Understand-scores-and-truncation","page":"Boosting","text":"The raw ensemble score is f0 + eta * sum(tree scores). Call score(model, X; clip = false) to obtain that sum. With truncate = true, score clamps the sum to bounds derived from the training target. predict applies the loss link after this clamp.\n\nTruncation also bounds numeric features inside each base tree. Set truncate = false to disable both forms of truncation. Inspect Interpreting models before comparing explanations with clipped predictions.\n\nFor Quantile and MAD, each round freezes one set of IRLS weights. Thus, each boosted round performs one IRLS step. See Losses for loss-specific score and response behavior.","title":"Understand scores and truncation"},{"category":"section","location":"#LinearTrees.jl","page":"Home","text":"LinearTrees fits decision trees with linear models along their paths. A tree can represent both smooth trends and abrupt changes. Gradient boosting combines these trees into an ensemble.\n\nUse the package for regression, classification, count prediction, and quantile regression on tabular data. It supports frequency weights, categorical features, exact and approximate split search, local coefficients, and SHAP values.","title":"LinearTrees.jl"},{"category":"section","location":"#Installation","page":"Home","text":"LinearTrees requires Julia 1.10 or later. Install the development version from GitHub in your project environment:\n\nusing Pkg\nPkg.add(url = \"https://github.com/BJMCox/LinearTrees.jl\")","title":"Installation"},{"category":"section","location":"#A-first-model","page":"Home","text":"Rows are observations and columns are features. Fit with fit_tree and evaluate new rows with predict:\n\nusing LinearTrees, Random\n\nrng = Xoshiro(42)\nX = rand(rng, 300, 3)\ny = 2 .* X[:, 1] .- X[:, 2] .+ 3 .* (X[:, 3] .> 0.6)\n\ntree = fit_tree(X, y; max_depth = 4)\nXnew = rand(rng, 5, 3)\nround.(predict(tree, Xnew); digits = 3)\n5-element Vector{Float64}:\n  1.679\n  2.074\n  4.448\n  3.499\n -0.244\n\nThe default loss is squared error. Choose another loss for probabilities, counts, positive responses, or conditional quantiles. Use fit_boost for an ensemble.","title":"A first model"},{"category":"section","location":"#Learn-the-package","page":"Home","text":"Goal Page\nFit and assess your first model Getting started\nUnderstand a tree and control its size Tree fitting\nTrain an ensemble with validation Boosting\nMatch the loss to your target Loss functions\nExplain fitted predictions Interpretation\nUse tables, MLJ, or saved models Interfaces and persistence\nChoose split search and threading Performance\nLook up a function or type API reference","title":"Learn the package"},{"category":"section","location":"#Model-scope","page":"Home","text":"Tree growth follows the PILOT approach, extended here to several losses and boosted ensembles. Linear terms are fitted one feature at a time. A prediction can involve several features because it sums terms along a path.\n\nThe package returns point predictions, class probabilities, or conditional quantiles according to the loss. It does not provide Bayesian posterior distributions, predictive intervals, missing-value imputation, or pruning. Class probabilities do not carry a calibration guarantee.\n\nThe original algorithm is described by Raymaekers, Rousseeuw, Verdonck, and Yao (2024), Fast linear model trees by PILOT, Machine Learning 113, 6561–6610.","title":"Model scope"},{"category":"section","location":"#License-and-support","page":"Home","text":"LinearTrees uses the Apache License 2.0. Copyright 2026 Benjamin Cox.\n\nReport bugs and request features through the issue tracker.","title":"License and support"}]
}
